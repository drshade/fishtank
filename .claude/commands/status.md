---
description: Current tank status summary
---

Summarise the state of the tank. Read all of `memory/` and skim the
latest journal week first, then report:

- Livestock census + breeding status (with expected dates)
- The headline from the last 1-2 journal weeks
- What's pending: arriving orders, date-gated next steps (with dates)
- Active watch items

Bullets, not paragraphs. Compare any date-gated items against
`date +%F` and flag anything that's now actionable.
